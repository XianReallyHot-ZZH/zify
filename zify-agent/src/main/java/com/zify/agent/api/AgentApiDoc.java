package com.zify.agent.api;

/**
 * Agent 模块 api 层占位。
 * <p>
 * Facade 接口和跨模块 DTO / Command / Query / Result。其他子模块通过 Maven 依赖消费本包下的公共 API。
 */
final class AgentApiDoc {

    private AgentApiDoc() {
    }
}
